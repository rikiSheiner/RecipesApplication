<UserControl x:Class="RecipesWpfApp.Views.ImagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RecipesWpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <Grid>

        <StackPanel>
            <Button x:Name="BrowseButton"  Content="browse image" Height="30" Width="80"
                    Command="{Binding BrowseImageCommand}"></Button>
            <Button x:Name="UploadButton"  Content="upload image" Height="30" Width="80"
                    Command="{Binding UploadImageCommand}"></Button>
            <Image x:Name="recipeImage" Width="200" Height="200" DataContext="{Binding ImageToAdd.ImageFile}"/>
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Image Source="{Binding Images}"></Image>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        
    </Grid>
</UserControl>
